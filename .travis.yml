sudo: false

language: java

jdk:
  - oraclejdk9
  - oraclejdk8
  - openjdk7

before_install: cd wffweb && export MAVEN_OPTS="-XX:+PrintCompilation"

install: mvn -B install -U -DskipTests=true

script: mvn -B verify -U -Dmaven.javadoc.skip=true

branches:
  only:
    - master
    - develop
